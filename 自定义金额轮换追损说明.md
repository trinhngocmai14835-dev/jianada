# 自定义金额轮换追损说明

## 入口

左侧菜单：自定义金额轮换追损

API 前缀：`/api/custom-rotatebet`

任务 ID：`custom_rotatebet`

## 配置规则

- `amount_steps` 为自定义金额阶梯，支持 1 到 20 阶，每一阶为正整数。
- 第一球、第二球、第三球各自独立启用或关闭。
- 每一路都有 A/B 两组号码，每组必须填写 4 个或 5 个有效号码，号码范围为 0 到 9。
- 入场触发支持直接开始、一次不中、两次不中、三次不中。
- 命中后回到第一阶；非直接开始模式会重新进入入场观察。
- 最后一阶未中后自动回到第一阶继续下一把。

## 运行规则

- 该模式独立于原有“轮换追损”，配置键、任务 ID、日志通道都不共用。
- 运行中保存新增账号后，新账号会单独启动浏览器，登录后等待下一完整周期再加入下注。
- 页面右侧可以单独停止某个账号，不影响其他账号继续运行。
- 任务管理器会阻止不同模式复用同一账号或同一浏览器调试端口。

## 验证范围

- 后端状态和解析测试覆盖金额阶梯、A/B 号码 4/5 个限制、三路独立入场观察。
- 后端烟测覆盖真实循环中的自定义金额阶梯推进。
- 任务管理测试覆盖账号和端口冲突保护。
