# 自动下单系统Pro — 安装与使用说明

## 一、安装前准备

### 1. 电脑要求
- **操作系统**：Windows 10 或 Windows 11（64位）
- **浏览器**：必须安装 **Google Chrome**（不能用Edge、Firefox代替）
  - 下载地址：https://www.google.cn/chrome/
- **网络**：能正常访问下单网站

### 2. 获取文件
你会收到一个名为 `自动下单系统Pro.exe` 的文件，把它放到你想要的文件夹里（建议放在桌面或D盘，**路径不能有中文和空格**，例如 `D:\BettingPro\` 是可以的，`D:\我的软件\` 会出问题）。

---

## 二、第一次启动

### 1. 双击运行 `自动下单系统Pro.exe`

- 第一次运行 Windows 可能弹出安全提示，点击 **"更多信息"** → **"仍要运行"**
- 稍等几秒，电脑会自动弹开 **Chrome 浏览器**，显示系统界面

> 如果没有自动弹出浏览器，手动打开Chrome，地址栏输入 `http://localhost:8080` 回车

### 2. 激活授权

首次打开会看到 **"未激活"** 提示，需要先激活：

1. 在页面左侧菜单点击 **"授权管理"**
2. 找到 **"机器码"** 那一行，把那串字母数字**完整复制**发给管理员
3. 管理员会给你一串**授权码**（格式类似 `ABCDE-FGHIJ-KLMNO-PQRST`）
4. 把授权码粘贴到"激活码"输入框，点击 **"立即激活"**
5. 显示"授权有效，剩余XX天"即激活成功

> 注意：授权码绑定的是你这台电脑，换电脑需要重新申请

---

## 三、三种下单模式说明

系统有三种模式，左侧菜单分别对应：

| 菜单名 | 模式 | 适合场景 |
|--------|------|---------|
| 自动下单 | 多路多球，分层止盈止损 | 稳定运营 |
| 赢冲输缩 | 3路4球，赢了加注输了减注 | 进攻型玩法 |
| 跟投下单 | 跟随主账号同步下注 | 多账号同步 |

---

## 四、自动下单 / 赢冲输缩 使用步骤

两种模式操作方法相同，以下以**赢冲输缩**为例：

### 第一步：填写配置

点击左侧菜单 **"赢冲输缩"**，填写以下内容：

| 字段 | 填写说明 |
|------|---------|
| 网站地址 | 下单网站的网址，例如 `https://166.tt` |
| 安全码 | 网站登录时需要的安全码，没有就留空 |
| 账号 | 你的下注账号用户名 |
| 密码 | 你的下注账号密码 |
| 调试端口 | 默认 `9222`，一般不用改；多账号时每个账号填不同的数字（9222、9223、9224…） |
| 一阶注码 | 输的时候用的注码金额 |
| 二阶注码 | 赢的时候冲的注码金额 |
| 每日止损 | 亏损到这个金额自动停止 |
| 止赢 | 盈利到这个金额自动停止 |

填好后点击 **"保存配置"** 按钮。

### 第二步：启动运行

点击 **"开始赢冲输缩"** 按钮：

1. 电脑会自动弹出一个 **Chrome 浏览器窗口**，打开下单网站
2. 系统会自动登录你填写的账号（首次可能需要几秒）
3. 登录成功后，进入游戏页面，系统开始自动监控并下注
4. 页面下方的**实时日志**会滚动显示每一步操作

> **重要**：Chrome 弹出后不要手动关闭它！系统靠这个窗口操作网站。最小化是可以的。

### 第三步：停止运行

点击 **"停止赢冲输缩"** 按钮，系统会在当期结束后安全退出，Chrome 窗口自动关闭。

---

## 五、跟投下单 使用步骤

跟投采用 **按客户金额倍数·镜像全跟**：每个跟投账号跟随采集账号（客户）的全部注单，
每注金额 = 客户该注金额 × 该账号的倍数（客户金额不固定，跟投自动随之变动）。

### 第一步：填写配置

| 字段 | 填写说明 |
|------|---------|
| 网站地址 | 下单网站网址 |
| 采集端口（主账号） | 被跟随账号Chrome的调试端口，默认 `9222` |
| 刷新间隔(秒) | 轮询客户注单的间隔，越小跟得越贴身，建议 `2~3` 秒 |
| 跟投账号 | 可添加多个，每个填 **CDP端口**（如 9223、9224，不可重复）和 **跟投倍数**（如 2 = 两倍、1 = 等额跟、支持小数） |

### 第二步：打开各账号浏览器（每个账号一个独立端口的Chrome）

1. **采集账号**：点 **"打开主账号浏览器"** → 弹出Chrome，手动登录采集账号，
   并打开 **"报表查询 → 注单明细"（未结明细）页**，日期选今天。
   👉 该页可在客户还没下注时就提前打开（显示无数据也没关系），程序会一直挂着等。
2. **每个跟投账号**：在对应行点 **"打开浏览器"** → 弹出Chrome，手动登录该跟投账号，停在下注盘口页。
   ⚠️ 必须用按钮弹出的Chrome登录，不能用平时自己开的普通Chrome（那个没带调试端口，连不上）。

### 第三步：启动跟投

确认各账号浏览器都已登录就绪后，点 **"开始跟投"**。客户一下注，程序就按倍数贴身跟投，
日志会实时显示，如 `✅ [9223] 2.0倍 跟客户5注 [...]`。

> 若日志报 `无可用的跟投账号 / ECONNREFUSED 127.0.0.1:9223`，说明对应端口的跟投浏览器没打开或没登录，
> 按第二步把它开起来再启动即可。

---

## 六、日常使用流程

```
每天使用：
1. 双击 自动下单系统Pro.exe
2. 等待浏览器自动打开（约2-3秒）
3. 选择模式 → 确认配置正确 → 点击"开始"
4. 最小化浏览器，让程序在后台自动运行
5. 不用时点击"停止"，然后关闭 .exe 窗口
```

---

## 七、常见问题

**Q：双击后闪一下就消失了？**
> 可能是端口被占用。检查任务管理器里有没有上次没关的 `自动下单系统Pro.exe`，右键结束任务，再重新双击。

**Q：点击开始后没有弹出Chrome？**
> 确认电脑安装了 Google Chrome（不是Edge）。

**Q：Chrome弹出来了但没有自动登录？**
> 检查账号密码是否填写正确，网站地址是否能正常打开。

**Q：日志一直显示"等待下注窗口"？**
> 正常现象，系统在等待下注时间段（倒计时60-120秒之间才会下注）。

**Q：提示"授权码已过期"？**
> 联系管理员续费，需要提供机器码重新生成授权码。

**Q：换了电脑能用吗？**
> 不能直接用，授权码绑定机器，换电脑需要重新申请激活。

---

## 八、注意事项

- 运行期间**不要关闭**被程序打开的Chrome窗口（可以最小化）
- 不要在程序运行的Chrome里手动操作，会干扰自动下单
- 同一台电脑只能运行**一个**自动下单系统Pro，不能开两个
- 软件数据存放在 `C:\用户\你的用户名\.betting_platform\` 文件夹，不要手动删除
